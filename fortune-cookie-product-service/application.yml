server:
  port: 8083

spring:
  application:
    name: ProductService

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 4500

management:
  port: 8003